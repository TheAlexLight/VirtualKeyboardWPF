<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:KeyboardPanelLibrary">

    <!--<Style x:Key="keyboardStyle" TargetType="{x:Type local:Keyboard}">
        <Setter Property="Margin" Value="5"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:Keyboard}">
                    <ListBox ItemsSource="{Binding KeyList, RelativeSource={RelativeSource AncestorType=local:Keyboard}, Mode=TwoWay}"
                                 Background="Gray"
                                 Height="{TemplateBinding Height}"
                             Margin="10">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Button Background="Purple" Margin="5"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->
</ResourceDictionary>